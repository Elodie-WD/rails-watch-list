<div class="container">
<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(../../images/);">
  <div class="container">
    <h1>Save any kind movies</h1>
  </div>
</div>

<div></div>

<h1>My List</h1>

<ul>
  <% @lists.each do |list| %>
    <li><%= link_to list.name, list_path(list) %></li>
  <% end %>
</ul>

<%= link_to "Create a new List", new_list_path %>
</div>
